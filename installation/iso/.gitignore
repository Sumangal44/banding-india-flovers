# archiso build artifacts and the build.sh staging tree.
/work/
/out/
/staging/
# persistent package cache for the offline closure bake (offline-repo.sh);
# reused across builds so a rebuild is a delta, not a full re-download.
/offline-cache/
